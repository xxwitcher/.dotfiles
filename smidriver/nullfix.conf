# Dotfiles: preload a shim that makes evdi_open_attached_to(NULL) safe, so
# SMIUSBDisplayManager doesn't crash against upstream libevdi. See
# smidriver/evdi-nullfix.c in the dotfiles repo.
[Service]
Environment=LD_PRELOAD=/usr/local/lib/libevdi-nullfix.so
